# Database configuration
POSTGRES_USER="user"
POSTGRES_PASSWORD="password"
POSTGRES_DB="db"
POSTGRES_PORT="5432"
APP_PORT="8000" # Used when running docker compose (host port)

DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public" # For Prisma

# System configuration
BASE_URL="http://localhost:4000" # Matches `npm run dev`; override in production deployments
CORE_AUTH_BASE_URL="http://localhost:3000"
CORE_AUTH_STATIC_KID="default"
